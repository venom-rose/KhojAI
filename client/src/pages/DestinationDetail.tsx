import { ArrowLeft, ArrowUpRight, CalendarDays, Car, Check, CircleAlert, CircleCheck, Home, Leaf, MapPin, ShieldCheck, Sparkles, Utensils, Users, Waves } from "lucide-react";
import { Link, useRoute } from "wouter";
import { ArrowLink, DestinationCard, SectionKicker, Shell, TrustScore } from "@/components/site";
import { getDestination, destinations } from "@/data/destinations";

export default function DestinationDetail() {
  const [, params] = useRoute("/destination/:slug");
  const destination = getDestination(params?.slug ?? "ziro");
  const nearby = destinations.filter((item) => item.slug !== destination.slug).slice(0, 3);
  const details = [["Best season", `${destination.bestSeason} · Plan around the clearest local window.`, CalendarDays], ["Estimated cost", `${destination.budget} · Approx. ₹12,000–18,000 for 5 days in this demo.`, Waves], ["Accessibility", "A considered route with a few slower transfers; confirm current access locally.", CircleAlert], ["Local experiences", "Community stays, terrace walks, seasonal food and small cultural encounters.", Leaf], ["How to reach", "Illustrative route notes will connect to live access data in a future version.", Car], ["Stay options", "Community stay, small guesthouse or homestay — availability is not live.", Home]] as const;
  const communitySignals = [{ label: "Traveler contributions", value: "18", copy: "first-hand notes" }, { label: "Local insights", value: "07", copy: "community voices" }, { label: "Recently updated", value: "2 days ago", copy: "illustrative timestamp" }];
  const metrics = [["Information quality", destination.trustMetrics.sourceQuality], ["Freshness", destination.trustMetrics.recency], ["Community agreement", destination.trustMetrics.communityAgreement], ["Completeness", destination.trustMetrics.completeness]] as const;

  return <Shell><main className="pt-[76px]"><section className="relative overflow-hidden bg-ink text-white"><img src={destination.image} alt={`${destination.name} landscape`} className="absolute inset-0 h-full w-full object-cover opacity-55" /><div className="absolute inset-0 bg-gradient-to-t from-ink via-ink/35 to-ink/10" /><div className="container relative flex min-h-[570px] flex-col justify-end pb-12 pt-20 md:min-h-[650px] md:pb-16"><Link href="/discover" className="absolute left-5 top-8 inline-flex items-center gap-2 text-xs font-semibold text-white/65 transition hover:text-white md:left-12"><ArrowLeft size={15} /> Back to discover</Link><div className="max-w-4xl"><div className="mb-5 flex flex-wrap gap-2"><span className="rounded-full border border-white/25 bg-white/10 px-3 py-1.5 text-[10px] font-mono uppercase tracking-[.16em] text-white/75">{destination.region}</span><span className="rounded-full border border-white/25 bg-white/10 px-3 py-1.5 text-[10px] font-mono uppercase tracking-[.16em] text-white/75">Community verified · demo</span></div><h1 className="font-display text-7xl tracking-[-.08em] md:text-9xl">{destination.name}</h1><div className="mt-4 flex flex-wrap items-center gap-4 text-sm text-white/70"><span className="flex items-center gap-2"><MapPin size={15} className="text-saffron-light" /> {destination.state}, India</span><span className="h-1 w-1 rounded-full bg-white/35" />{destination.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><div className="mt-8 flex flex-wrap items-center gap-3"><TrustScore score={destination.trustScore} /><Link href="/planner" className="inline-flex items-center gap-2 rounded-full bg-white px-4 py-2.5 text-xs font-semibold text-ink transition hover:-translate-y-0.5 hover:bg-saffron-light">Plan a trip here <ArrowUpRight size={14} /></Link></div></div></div></section><section className="bg-ink py-16 text-white md:py-24"><div className="container"><div className="grid gap-10 lg:grid-cols-[.8fr_1.2fr] lg:items-center"><div><SectionKicker light>Destination intelligence · Demo layer</SectionKicker><h2 className="max-w-xl font-display text-5xl leading-[.98] tracking-[-.06em] md:text-6xl">See the signal behind the <em className="text-saffron-light">scenery.</em></h2><p className="mt-6 max-w-md text-sm leading-7 text-white/55">Hidden India AI does not reduce a place to a star rating. It makes the strength of the available information visible before you decide whether the destination fits.</p><div className="mt-8 flex items-center gap-3 text-xs text-white/45"><Sparkles className="text-saffron-light" size={16} /> A transparent signal, not a promise.</div></div><div className="rounded-[28px] border border-white/15 bg-white/[.07] p-6 shadow-[0_28px_70px_rgba(0,0,0,.24)] backdrop-blur-sm md:p-8"><div className="flex flex-col gap-6 border-b border-white/10 pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.2em] text-saffron-light">Destination intelligence</p><p className="mt-3 font-display text-7xl leading-none tracking-[-.08em]">{destination.trustScore}<span className="ml-2 text-2xl tracking-[-.04em] text-white/35">/100</span></p><p className="mt-2 text-xs text-white/45">Trust Score · mock intelligence</p></div><div className="inline-flex items-center gap-2 rounded-full bg-olive/35 px-3 py-1.5 text-xs font-semibold text-white"><ShieldCheck size={14} /> Community verified</div></div><div className="space-y-5 pt-6">{metrics.map(([label, value]) => <div key={label}><div className="mb-2 flex items-center justify-between gap-4 text-xs"><span className="text-white/65">{label}</span><span className="font-semibold text-white">{value}</span></div><div className="h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-saffron-light transition-all duration-500" style={{ width: `${value}%` }} /></div></div>)}</div><p className="mt-6 border-t border-white/10 pt-5 text-[11px] leading-5 text-white/40">Trust Score reflects the quality, consistency, freshness and completeness of available destination information. It is not a guarantee of safety or experience.</p></div></div></div></section><section className="container py-16 md:py-24"><div className="grid gap-14 lg:grid-cols-[1.1fr_.75fr] lg:items-start"><div><SectionKicker>What we know · Demo values</SectionKicker><h2 className="max-w-2xl font-display text-5xl leading-[1] tracking-[-.06em] md:text-6xl">Useful context before you <em className="text-olive">go deeper.</em></h2><p className="mt-7 max-w-2xl text-base leading-8 text-ink/65">{destination.description} This is the context layer around the destination — the details that help a traveller decide if the place fits their pace, interests and practical comfort.</p><div className="mt-10 grid gap-px overflow-hidden rounded-[24px] border border-line bg-line sm:grid-cols-2">{details.map(([title, body, Icon]) => <div key={title} className="bg-white p-6"><Icon size={18} className="text-saffron" /><h3 className="mt-5 text-sm font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-ink/55">{body}</p></div>)}</div><div className="mt-7 rounded-[22px] border border-saffron/25 bg-saffron/5 p-5"><div className="flex gap-3"><CircleAlert size={17} className="mt-0.5 shrink-0 text-saffron" /><div><p className="text-sm font-semibold text-ink">Travel with context</p><p className="mt-1 text-xs leading-5 text-ink/55">{destination.demoNote} Local access, weather, permissions and community guidance should always be verified close to departure.</p></div></div></div></div><div className="lg:sticky lg:top-28"><div className="rounded-[24px] border border-line bg-mist p-6"><div className="flex items-start justify-between gap-4"><div><SectionKicker>Community signals</SectionKicker><h3 className="font-display text-3xl tracking-[-.05em]">What people keep noticing.</h3></div><Users className="mt-1 text-olive" size={20} /></div><div className="mt-7 space-y-3">{communitySignals.map((signal) => <div key={signal.label} className="flex items-center justify-between gap-4 rounded-2xl border border-white/80 bg-white p-4"><div><p className="text-xs font-semibold text-ink">{signal.label}</p><p className="mt-1 text-[11px] text-ink/45">{signal.copy}</p></div><p className="font-display text-2xl tracking-[-.04em] text-olive">{signal.value}</p></div>)}</div><div className="mt-4 flex items-center gap-2 rounded-full bg-olive px-3 py-2 text-[11px] font-semibold text-white"><Check size={13} /> Community verified · illustrative</div></div><div className="mt-5 rounded-[24px] bg-ink p-6 text-white"><p className="font-mono text-[10px] uppercase tracking-[.2em] text-saffron-light">Community pulse</p><p className="mt-4 font-display text-3xl leading-tight tracking-[-.04em]">“A little more time here is almost always a good idea.”</p><p className="mt-5 text-xs text-white/45">Pattern from 18 illustrative notes · demo only</p></div></div></div></section><section className="bg-mist py-16 md:py-20"><div className="container"><div className="mb-8 flex items-end justify-between gap-4"><div><SectionKicker>Keep looking around</SectionKicker><h2 className="font-display text-4xl tracking-[-.05em]">Nearby, not identical.</h2></div><ArrowLink href="/discover">See all places</ArrowLink></div><div className="grid gap-5 md:grid-cols-3">{nearby.map((item) => <DestinationCard key={item.slug} destination={item} />)}</div></div></section></main></Shell>;
}
