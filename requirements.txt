# ==============================================================================
# KHOJAI Python Dependencies (root-level for Vercel serverless runtime)
# ==============================================================================
# Vercel's Python runtime discovers this file at the project root to install
# packages before invoking api/index.py.
#
# This file mirrors backend/requirements.txt intentionally.
# If you add a dependency to backend/requirements.txt, add it here too.
# ==============================================================================

# Core Framework & Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database & ORM
sqlalchemy>=2.0.35
alembic>=1.13.3
asyncpg>=0.29.0
aiosqlite>=0.20.0

# Validation & Settings
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Security & Authentication
pyjwt[crypto]>=2.9.0
bcrypt>=4.2.0
python-multipart>=0.0.9

# HTTP client (for Amadeus / Google Places adapters)
httpx>=0.27.0
